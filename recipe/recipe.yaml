# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "4.11.15"
  python_check_max: "3.14"

package:
  name: platformdirs
  version: ${{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/p/platformdirs/platformdirs-${{ version }}.tar.gz
  sha256: d7419e973b2b740d428200130f80c0e79304d1c71081001db911e15b26a3a6d4

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check --check-build-dependencies

requirements:
  host:
    - hatch-vcs >=0.5
    - hatchling >=1.29
    - pip
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}

tests:
  - package_contents:
      strict: true
      site_packages:
        - platformdirs/**
        - platformdirs-${{ version }}.dist-info/**
  - python:
      imports: platformdirs
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - ${{ python_check_max }}.*
  - requirements:
      run:
        - mypy
        - python ${{ python_min }}.*
    script:
      - mypy -p platformdirs --ignore-missing-imports --disable-error-code=safe-super ${{ "--exclude=windows" if unix }}

about:
  homepage: https://pypi.org/project/platformdirs
  license_file: LICENSE
  license: MIT
  summary: A small Python module for determining appropriate platform-specific dirs, e.g. a "user data dir".
  repository: https://github.com/tox-dev/platformdirs

extra:
  recipe-maintainers:
    - adament
    - gaborbernat
    - bollwyvl
